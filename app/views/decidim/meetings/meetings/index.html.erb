<!--  Fehler mit search.result.not_online.exists 
      orginal Seite 
      Projekt_Komponente/mainhardt-decidim-partizipationshub/decidim-meetings/app/views/decidim/meetings/meetings/index.html.erb -->
      <div class="columns medium-7 mediumlarge-8 process-header__info">
      <%= render partial: "decidim/shared/component_announcement" %>
      
      </div>

<div class="row">

  <div class="columns mediumlarge-4 large-3">

  <% if false %>
  <nav aria-label="You are here:" role="navigation">
    <ul class="breadcrumbs text">
      <li><%= link_to translated_attribute(current_participatory_space.title), decidim_assemblies.assembly_path(current_participatory_space) %></li>
      <li><%= t(".back")%></li>
    </ul>
  </nav>
  <% end %>
              
  <h1 class="heading2 text">
    <%= link_to translated_attribute(current_participatory_space.title), decidim_assemblies.assembly_path(current_participatory_space), class: "heading2"  %>
  </h1>

    

    <%= render partial: "filters_small_view" %>
    <div class="card card--secondary show-for-mediumlarge">
      <%= render partial: "filters" %>
    </div>
  </div>
  <div id="meetings" class="columns mediumlarge-8 large-9" aria-live="polite">

    <%= render partial: "meetings" %>
  </div>
</div>
<div class="row columns">
  <div class="title-action">
  <!--
    <h2 id="meetings-count" class="title-action__title section-heading">
      <%= render partial: "count" %>
    </h2>
  -->

    <% if allowed_to?(:create, :meeting) %>
    <div class="text">
      <%= action_authorized_link_to :create, new_meeting_path, class: "title-action__action button small", data: { "redirect_url" => new_meeting_path } do %>
        <%= t("new_meeting", scope: "decidim.meetings.meetings.index") %>
        <%= icon "plus", role: "img", "aria-hidden": true %>
      <% end %>
    </div>
    <% end %>
    
  </div>

</div>
